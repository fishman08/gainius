# Use a flat node_modules layout. React Native + Expo's bundler require
# transitive deps (invariant, @babel/runtime, expo-asset, etc.) to be
# directly resolvable; pnpm's default symlinked layout breaks this.
node-linker=hoisted
strict-peer-dependencies=false
